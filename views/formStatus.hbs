<title>Booking Status</title>
<link rel="stylesheet" href="../css/styles.css">
<body>
    <div class="status-container">
        <h2>Booking Status</h2>
        <div class="status-info">
            <div class="status-item">
                <span class="label">Reference Code:</span>
                <span class="value">{{form.referenceCode}}</span>
            </div>
            <div class="status-item">
                <span class="label">Pickup Location:</span>
                <span class="value">{{form.pickupAddress}}</span>
            </div>
            <div class="status-item">
                <span class="label">Destination:</span>
                <span class="value">{{form.destinationAddress}}</span>
            </div>
            <div class="status-item">
                <span class="label">Pickup Date:</span>
                <span class="value">{{form.pickupDate}}</span>
            </div>
            <div class="status-item">
                <span class="label">Pickup Time:</span>
                <span class="value">{{form.pickupTime}}</span>
            </div>
            <div class="status-item">
                <span class="label">Departure Date:</span>
                <span class="value">{{form.departureDate}}</span>
            </div>
            <div class="status-item">
                <span class="label">Departure Time:</span>
                <span class="value">{{form.departureTime}}</span>
            </div>
            <div class="status-item">
                <span class="label">Passengers:</span>
                <span class="value">{{form.pickupPassengers}}</span>
            </div>
            <div class="status-item">
                <span class="label">Status:</span>
                {{#ifCond form.status '==' 'confirmed'}}
                    <span class="value" style="color: green;">Confirmed</span> 
                {{else ifCond form.status '==' 'processing'}}
                    <span class="value" style="color: rgb(201, 201, 24);">Processing</span>
                {{else ifCond form.status '==' 'cancelled'}}
                    <span class="value" style="color: red;">Cancelled</span>
                {{else}}
                    <span class="value">{{form.status}}</span>
                {{/ifCond}}
            </div>
        </div>
        <div class="status-message">
            {{#ifCond form.status '==' 'confirmed'}}
                Your booking has been confirmed. We look forward to serving you.
            {{else ifCond form.status '==' 'processing'}}
                Your booking is in progress. We are processing your request and will notify you shortly.
            {{else ifCond form.status '==' 'cancelled'}}
                We're sorry to inform you that your booking has been cancelled. Please contact us for more details.
            {{else}}
                Your booking status is: {{form.status}}. Please check back later for updates.
            {{/ifCond}}
        </div>
        <button class="back-button" onclick="window.location.href='/'">Back to Options</button>
    </div>
</body>
